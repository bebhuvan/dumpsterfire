---
export interface Props {
  issueNumber?: number;
}

const { issueNumber } = Astro.props;

const currentDate = new Date().toLocaleDateString('en-US', { 
  month: 'long', 
  day: 'numeric', 
  year: 'numeric' 
});
---

<header class="header">
  <div class="header-top">
    <div>
      <h1 class="site-title sans">
        <a href="/"><span class="site-logo">🔥</span> FROM THE DUMPSTER FIRE</a>
      </h1>
      <p class="site-subtitle">Rescuing signal from the algorithmic inferno</p>
    </div>
    <nav class="main-nav sans">
      <a href="/archive">Archive</a>
      <a href="/search">Search</a>
      <a href="/about">About</a>
      <a href="/subscribe">Subscribe</a>
      <a href="/rss.xml" class="rss-link" title="RSS Feed">RSS 📡</a>
    </nav>
  </div>
  {issueNumber && (
    <div class="timestamp">
      Issue {String(issueNumber).padStart(3, '0')} · {currentDate}
    </div>
  )}
</header>